// Generated by XSLT code generator
#ifndef __RENDERER_H__
#define __RENDERER_H__

#include <include/orca.h>

typedef struct window window_t;
typedef struct Texture Texture_t;
typedef struct Image Image_t;
typedef struct RenderTargetTexture RenderTargetTexture_t;
typedef struct CubeMapTexture CubeMapTexture_t;
typedef struct IOSurfaceTexture IOSurfaceTexture_t;
typedef struct VertexShader VertexShader_t;
typedef struct FragmentShader FragmentShader_t;
typedef struct Shader Shader_t;
typedef struct Material Material_t;
typedef struct Mesh Mesh_t;
typedef struct FontFamily FontFamily_t;
typedef struct Trajectory Trajectory_t;
typedef struct Timeline Timeline_t;

// Window interface abstraction.
struct window {
};

// Base class for managing texture resources and their sampling parameters for rendering.
struct Texture {
    uint IOSurface;
    uint texnum;
    uint framebuffer;
    uint depthbuffer;
    bool loaded;
    TextureFilter MinificationFilter;
    TextureFilter MagnificationFilter;
    TextureWrap WrapMode;
    TextureFormat Format;
    int Scale;
    MipmapMode MipmapMode;
    AnisotropyType AnisotropyType;
    int Width;
    int Height;
};

// Represents a single, pre-loaded texture image for use in rendering operations.
struct Image {
    Texture_t base;
    fixed Source;
    bool PremultiplyAlpha;
    ImageType Type;
    bool HasMipmaps;
    fixed Size;
    int BitDepth;
    bool FileHasTransparency;
    bool FileHasICCProfile;
    ImageFormat TargetFormat;
    AtcCompressionScheme AtcCompressionScheme;
    int PvrtcBitDepth;
    int PvrtcQuality;
    int PvrtcCompressionScheme;
    AstcFormat AstcBlockSize;
    AstcCompressionSpeed AstcCompressionSpeed;
    bool AstcIsSRGB;
    RawColorByteFormat RawColorByteFormat;
    SpansionCompressionScheme SpansionCompressionScheme;
    FilePngCompressionLevel FilePngCompressionLevel;
    int Etc2Effort;
    bool FileExportSourceTexture;
    bool FileExportAlways;
    bool FileExportEmptyWhenFileIsMissing;
    fixed FileExportSourceImage;
};

// Texture configured as a render target for off-screen rendering and post-processing effects.
struct RenderTargetTexture {
    Texture_t base;
    int Width;
    int Height;
    RenderTargetType TargetType;
    bool SupportSimpleRenderTarget;
    RenderTargetTextureAttachment Attachment;
    bool AllowDepthFallback;
    int SampleCount;
};

// Cubemap texture assembled from six individual images, one for each face of a cube, used for environment mapping, reflections, and sky rendering.
struct CubeMapTexture {
    Texture_t base;
    fixed BackImage;
    fixed FrontImage;
    fixed LeftImage;
    fixed RightImage;
    fixed BottomImage;
    fixed TopImage;
};

// Texture created from a shared macOS IOSurface for zero-copy GPU data sharing.
struct IOSurfaceTexture {
    Texture_t base;
    int IOSurface;
};

// Vertex processing stage of the shader pipeline
struct VertexShader {
    int Version;
    FloatPrecision FloatPrecision;
    Shading Shading;
};

// Fragment (pixel) processing stage of the shader pipeline
struct FragmentShader {
    int Version;
    FloatPrecision FloatPrecision;
    fixed Out;
};

// Complete shader program definition combining vertex and fragment stages
struct Shader {
    shader* shader;
    BlendMode BlendMode;
    CompareFunc DepthTestFunction;
    bool DepthWriteEnabled;
};

// Shader-based rendering material for advanced visual effects
struct Material {
    Shader Shader;
    color GlobalAmbient;
    color Ambient;
    color Diffuse;
    color Emissive;
    color SpecularColor;
    float SpecularExponent;
    Texture Texture;
    Texture Texture2;
    vec2 TextureOffset;
    vec2 TextureTiling;
    float BlendIntensity;
    BlendMode BlendMode;
    color u_color;
    Texture u_texture;
};

// Renderable 3D geometry component with material and model data
struct Mesh {
    model* model;
    fixed Source;
    Material Material;
    fixed MeshMorphTargets;
};

// TrueType font resource for text rendering
struct FontFamily {
    fontface* regular;
    fontface* bold;
    fontface* italic;
    fontface* bolditalic;
    fixed Regular;
    fixed Bold;
    fixed Italic;
    fixed BoldItalic;
};

// 
struct Trajectory {
};

// 
struct Timeline {
};

#endif
