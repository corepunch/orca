// Generated by XSLT code generator
#ifndef __UIKIT_H__
#define __UIKIT_H__

#include <include/orca.h>

#include <source/renderer/api/renderer.h>
#include <include/renderer.h>
#include <source/UIKit/uidef.h>
typedef struct BorderRadiusShorthand BorderRadiusShorthand_t;
typedef struct BorderEdgeShorthand BorderEdgeShorthand_t;
typedef struct BorderSideShorthand BorderSideShorthand_t;
typedef struct BorderShorthand BorderShorthand_t;
typedef struct EdgeShorthand EdgeShorthand_t;
typedef struct AlignmentShorthand AlignmentShorthand_t;
typedef struct FontShorthand FontShorthand_t;
typedef struct BrushShorthand BrushShorthand_t;
typedef struct ShadowShorthand ShadowShorthand_t;
typedef struct RingShorthand RingShorthand_t;
typedef struct OverflowShorthand OverflowShorthand_t;
typedef struct UnderlineShorthand UnderlineShorthand_t;
typedef struct Thickness Thickness_t;
typedef struct MarginShorthand MarginShorthand_t;
typedef struct SizeAxisShorthand SizeAxisShorthand_t;
typedef struct SizeShorthand SizeShorthand_t;
typedef struct DataObject DataObject_t;
typedef struct AnimationPlayer AnimationPlayer_t;
typedef struct Trigger Trigger_t;
typedef struct OnPropertyChangedTrigger OnPropertyChangedTrigger_t;
typedef struct OnAttachedTrigger OnAttachedTrigger_t;
typedef struct EventTrigger EventTrigger_t;
typedef struct Setter Setter_t;
typedef struct Handler Handler_t;
typedef struct Brush Brush_t;
typedef struct ColorBrush ColorBrush_t;
typedef struct Node Node_t;
typedef struct TextRun TextRun_t;
typedef struct TextBlockConcept TextBlockConcept_t;
typedef struct Node2D Node2D_t;
typedef struct PrefabView2D PrefabView2D_t;
typedef struct TextBlock TextBlock_t;
typedef struct Input Input_t;
typedef struct Button Button_t;
typedef struct Label Label_t;
typedef struct StackView StackView_t;
typedef struct Form Form_t;
typedef struct CollectionView CollectionView_t;
typedef struct Control Control_t;
typedef struct Screen Screen_t;
typedef struct Cinematic Cinematic_t;
typedef struct Grid Grid_t;
typedef struct ImageView ImageView_t;
typedef struct NinePatchImage NinePatchImage_t;
typedef struct TerminalView TerminalView_t;
typedef struct Style Style_t;

// Corner rounding configuration for rectangular elements
struct BorderRadiusShorthand {
    float TopLeftRadius;
    float TopRightRadius;
    float BottomRightRadius;
    float BottomLeftRadius;
};

// Complete border definition for one edge
struct BorderEdgeShorthand {
    float Width;
    BorderStyle Style;
    color Color;
};

// Border definitions for both sides of an axis
struct BorderSideShorthand {
    BorderEdgeShorthand Left;
    BorderEdgeShorthand Right;
};

// Complete border definition for rectangular element
struct BorderShorthand {
    BorderRadiusShorthand Radius;
    BorderSideShorthand Axis[3];
};

// Spacing values for opposite edges of an axis
struct EdgeShorthand {
    float Left;
    float Right;
};

// Axis-specific alignment parameters.
struct AlignmentShorthand {
    int Axis[3];
};

// Complete font specification including weight, style, size, and family
struct FontShorthand {
    FontWeight Weight;
    FontStyle Style;
    float Size;
    FontFamily* Family;
};

// Represents a generic brush used for filling UI elements.
struct BrushShorthand {
    color Color;
    Texture* Image;
    Material* Material;
};

// Drop shadow effect configuration
struct ShadowShorthand {
    vec2 Offset;
    float BlurRadius;
    float SpreadRadius;
    color Color;
};

// Decorative ring/outline effect around elements
struct RingShorthand {
    float Offset;
    float Width;
    color Color;
};

// Per-axis overflow behavior configuration
struct OverflowShorthand {
    Overflow x;
    Overflow y;
};

// Defines shorthand underline properties (offset, width, and color).
struct UnderlineShorthand {
    float Offset;
    float Width;
    color Color;
};

// Uniform thickness specification for all four edges
struct Thickness {
    float Top;
    float Right;
    float Bottom;
    float Left;
};

// External spacing configuration for rectangular elements
struct MarginShorthand {
    EdgeShorthand Axis[3];
};

// Complete sizing definition for one axis
struct SizeAxisShorthand {
    float Requested;
    float Min;
    float Actual;
    float Scroll;
};

// Sizing configuration of elements (e.g., Width, Height, Depth)
struct SizeShorthand {
    SizeAxisShorthand Axis[3];
};

// Base class for data-holding objects within the framework
struct DataObject {
};

// Controls playback of animations within the UI engine.
struct AnimationPlayer {
    bool AutoplayEnabled;
    float DurationScale;
    PlaybackMode PlaybackMode;
    bool RelativePlayback;
    int RepeatCount;
    bool RestoreOriginalValuesAfterPlayback;
    Timeline Timeline;
};

// Base class for event-driven actions within the UI system
struct Trigger {
    fixed Property;
    int Value;
};

// Responds to property value changes on specified nodes
struct OnPropertyChangedTrigger {
    fixed SourceNode;
    fixed Property;
};

// Triggers when an element is attached to the visual tree
struct OnAttachedTrigger {
};

// Responds to routed UI events (mouse, keyboard, etc.)
struct EventTrigger {
    fixed RoutedEvent;
};

// Applies property values when triggered by specified conditions
struct Setter {
    Trigger Trigger;
    fixed Property;
    fixed Value;
};

// Executes custom functions when triggered by specified conditions
struct Handler {
    Trigger Trigger;
    Object Target;
    fixed Function;
};

// Base class for all brush types used in rendering.
struct Brush {
};

// Brush that fills an area with a solid color.
struct ColorBrush {
    Brush_t base;
    color Color;
};

// Base class for all UI engine nodes.
struct Node {
    SizeShorthand Size;
    MarginShorthand Margin;
    MarginShorthand Padding;
    BorderShorthand Border;
    AlignmentShorthand Alignment;
    bool Visible;
    bool QuickHide;
    float VisibleAmountInParent;
    float Opacity;
    objectTags Tags;
    DataObject DataContext;
};

// Shared concept for text rendering in 2D and 3D nodes.
struct TextRun {
    uiLabelSize _size;
    text_info _textinfo;
    FontShorthand* _font;
    fixed Text;
    FontShorthand Font;
    UnderlineShorthand Underline;
    float LetterSpacing;
    float LineHeight;
    float CharacterSpacing;
    float FixedCharacterWidth;
    bool RemoveSideBearingsProperty;
};

// Shared concept for text rendering in 2D and 3D nodes.
struct TextBlockConcept {
    TextRun_t base;
    uiLabelSteps _steps;
    Node* _node;
    ViewText* _text;
    fixed TextResourceID;
    fixed TextResourceConfiguration;
    fixed PlaceholderText;
    fixed TextOverflow;
    BrushShorthand Placeholder;
    bool UseFullFontHeight;
    bool ConstrainContentHeight;
    bool WordWrap;
    TextHorizontalAlignment TextHorizontalAlignment;
    TextVerticalAlignment TextVerticalAlignment;
};

// Primary class for 2D UI elements with comprehensive rendering capabilities
struct Node2D {
    Node_t base;
    Node* _node;
    Object* _object;
    int _userdata;
    rect _rect;
    actualPos _actual_pos;
    float _opacity;
    int _bbox_flags;
    transform2 LayoutTransform;
    transform2 RenderTransform;
    vec2 RenderTransformOrigin;
    vec2 ContentOffset;
    mat4 Matrix;
    Texture RenderTarget;
    BrushShorthand Background;
    BrushShorthand Foreground;
    ShadowShorthand BoxShadow;
    OverflowShorthand Overflow;
    RingShorthand Ring;
    Material CompositionBrush;
    vec2 CompositionDesignSize;
    bool SizeToContent;
    bool OffscreenRendering;
    bool ForceComposition;
    bool CacheResult;
    bool SnapToPixel;
    bool ClipChildren;
    bool ContentStretch;
    bool Hovered;
    bool IgnoreHitTest;
    ForegroundHint ForegroundHint;
};

bool Node2D_IsFrameSet(Node2D_t* self, Box3Field field);

void Node2D_SetFrame(Node2D_t* self, Box3Field field, float value);

float Node2D_GetFrame(Node2D_t* self, Box3Field field);

void Node2D_Measure(Node2D_t* self, Direction direction, float availableSize, bool forceRemeasure);

void Node2D_Arrange(Node2D_t* self, bounds bounds, Direction direction);

bounds Node2D_GetBounds(Node2D_t* self, Direction axis);

rect Node2D_GetRect(Node2D_t* self);

rect Node2D_GetBackgroundRect(Node2D_t* self);

float Node2D_GetSize(Node2D_t* self, Direction axis, Sizing sizing);

// Container for loading and displaying prefabricated UI components
struct PrefabView2D {
    Node2D_t base;
    int _loadedSCA;
    int _loadedPrefab;
    fixed SCA;
    fixed Prefab;
};

// Concrete implementation of text display combining Node2D and TextBlockConcept
struct TextBlock {
    Node2D_t base;
    Node2D* _node2D;
};

// Interactive text input control with multiple input modes
struct Input {
    TextBlock_t base;
    Texture* _checkmark;
    fixed Name;
    InputType Type;
    int Cursor;
    bool Multiline;
    bool Checked;
};

// Interactive button control for user actions
struct Button {
    TextBlock_t base;
    ButtonType Type;
};

// Text label with optional association to form controls
struct Label {
    TextBlock_t base;
    fixed For;
};

// Linear layout container arranging children in a single direction
struct StackView {
    Node2D_t base;
    bool Reversed;
    Direction Direction;
    AlignItems AlignItems;
    JustifyContent JustifyContent;
    float Spacing;
};

// Specialized stack view for form input collection and submission
struct Form {
    StackView_t base;
};

// Container for dynamic collections of data-driven items
struct CollectionView {
    Node2D_t base;
    Direction Direction;
    float Spacing;
};

// Base class for interactive UI controls with state management
struct Control {
    Node2D_t base;
    bool Pressed;
    bool Disabled;
    bool Hovered;
    bool Focused;
    bool Value;
    float Scale;
};

// Top-level container representing a display surface or window
struct Screen {
    Node2D_t base;
    Texture* _rt;
    uint _size;
    bool _initialized;
    color ClearColor;
    ResizeMode ResizeMode;
};

// Video or animation playback view for media content
struct Cinematic {
    Node2D_t base;
    bool _playing;
    longTime _starttime;
    fixed FileName;
    float FrameRate;
    float NumFrames;
    float FadeOut;
};

// Grid-based layout container with configurable rows and columns
struct Grid {
    Node2D_t base;
    uiGridViewBuffer _buffer;
    fixed Columns;
    fixed Rows;
    Direction Direction;
    float Spacing;
    float CellWidth;
    float CellHeight;
};

// Displays image content with advanced rendering and scaling options
struct ImageView {
    Node2D_t base;
    int _loadedImage;
    fixed Src;
    Texture Image;
    vec4 Edges;
    vec4 Insets;
    vec4 Viewbox;
    Stretch Stretch;
};

// Specialized image view for nine-patch (stretchable) images
struct NinePatchImage {
    Node2D_t base;
    float StretchTypeTop;
    float StretchTypeBottom;
    float StretchTypeLeft;
    float StretchTypeRight;
    float StretchTypeCenter;
    Texture ImageTopLeft;
    Texture ImageTop;
    Texture ImageTopRight;
    Texture ImageLeft;
    Texture ImageCenter;
    Texture ImageRight;
    Texture ImageBottomLeft;
    Texture ImageBottom;
    Texture ImageBottomRight;
};

// Displays a fixed-size text grid using a monospace layout
struct TerminalView {
    Node2D_t base;
    int* _buffer;
    vec2 _scroll;
    int _contentHeight;
    int BufferWidth;
    int BufferHeight;
    int Cursor;
    int SelectedIndex;
    bool DropShadow;
};

// Defines a reusable set of property values and visual configurations for a target component type.
struct Style {
    fixed TargetType;
    StyleType Type;
};

#endif
