# OverflowShorthand

**Struct** | `orca.UIKit`

Per-axis overflow behavior configuration

## Overview

Controls how content that exceeds container bounds is handled for each axis independently.

## Fields

| Field | Type | Description |
|-------|------|-------------|
| `x` | `Overflow` | Horizontal overflow handling |
| `y` | `Overflow` | Vertical overflow handling |

