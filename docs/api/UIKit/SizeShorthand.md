# SizeShorthand

**Struct** | `orca.UIKit`

Sizing configuration of elements (e.g., Width, Height, Depth)

## Overview

You can set values and the engine will try it's best to preserve those.

## Fields

| Field | Type | Description |
|-------|------|-------------|
| `Axis` | `SizeAxisShorthand[3]` | A number value per axis |

