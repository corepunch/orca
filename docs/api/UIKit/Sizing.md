# Sizing

**Enum** | `orca.UIKit`

Size calculation modes for layout operations

## Constants

| Name | Description |
|------|-------------|
| `PlusMargin` | Include margins in size calculation |
| `SizeOnly` | Return only the base size |
| `MinusPadding` | Subtract padding from size |

