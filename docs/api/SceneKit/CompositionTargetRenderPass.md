# CompositionTargetRenderPass

**Class** | `orca.SceneKit` | extends [RenderPass](RenderPass.md)

Render pass that outputs to the final composition target

## Overview

Terminal render pass in the pipeline that writes to the final framebuffer or render target. Used as the last step in multi-pass rendering chains.

